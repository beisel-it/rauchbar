{
  "name": "@rauchbar/admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "build": "node -e \"console.log('admin build placeholder ready')\"",
    "lint": "echo \"admin lint pending\"",
    "start": "node server.mjs",
    "test": "echo \"admin tests pending\"",
    "typecheck": "echo \"admin typecheck pending\""
  }
}
